
@{
    ViewData["Title"] = "Friends";
}
@model List<UserAccount>
<br />
<br />
<br />
<div class="container">
    <div style="text-align: center; font-size: 34px; color: #b3cdff; font-family: serif;}">
        <p>Followings</p>
    </div>
    <div class="row row-space-2">
        @if (Model.Count > 0)
        {
            <div class="friendsGrid">
            @for (var i = Model.Count - 1; i >= 0; i--)
            {
                <div class="friend form_color"><partial name="_FollowingsPartialView" model="@Model[i]" /></div>
            }
            </div>
        }
        else
        {
            <div style="text-align: center; font-size: 20px; color: #b3cdff; font-family: serif;}">
                <p>No Friend to show</p>
        </div>
    }
    </div>
</div>
@section s2{
    <script type="text/javascript">  
        //$(document).ready(function () {
        //    $(function () {
        //        $('.removeUserButton').click(function (event) {
        //            var s = this.id;
		      //      $.post("/Home/Friends", { name : s }, function (data) {
        //                //alert(data);
		      //      });
        //        });
        //    });
        //});  
    </script>
}
@*<form  action="/Home/Friends" method="post">
                                    <a style="color:white; margin: 0px 0px 0px 11px;" id="RemoveUser" name="username">
                                        @Model[i].user.Fname @Model[i].user.Lname
                                    </a><br />
                                    <button type="submit" id="@Model[i].user.Fname @Model[i].user.Lname" 
                                    class="form-control btn btn-primary submit px-3 designLable removeUserButton" 
                                    style="margin: 2px 0px 0px 11px;">Remove</button>
                                </form>*@